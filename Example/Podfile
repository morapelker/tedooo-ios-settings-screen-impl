use_frameworks!

platform :ios, '13.0'

target 'SettingsScreenImpl_Example' do
  pod 'SettingsScreenImpl', :path => '../'

  target 'SettingsScreenImpl_Tests' do
    inherit! :search_paths

    pod 'Quick', '~> 2.2.0'
    pod 'Nimble', '~> 10.0.0'
  end
end
